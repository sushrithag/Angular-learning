import { ModifynamePipe } from './modifyname.pipe';

describe('ModifynamePipe', () => {
  it('create an instance', () => {
    const pipe = new ModifynamePipe();
    expect(pipe).toBeTruthy();
  });
});
